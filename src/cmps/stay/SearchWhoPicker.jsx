import { SearchPickerWrapper } from "./SearchPickerWrapper";

export function SearchWhoPicker({ type }) {
  return (
    <SearchPickerWrapper type={type}>
      <section className="search-who-picker">search-who-picker</section>
    </SearchPickerWrapper>
  );
}
